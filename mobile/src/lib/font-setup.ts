// Bengali Taka symbol (৳) font setup
// NotoSansBengali is loaded via useFonts() in _layout.tsx
// and applied explicitly via <PriceText> component for price rendering.
// The previous defaultProps approach didn't work with NativeWind className processing.
